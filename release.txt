.js here and min.js are linked. You can just change in WebAdmin/www/assets.

Then build docs and release /API via npm publish.
Then npm publish WebAdmin. Do not use local WebAdmin.min.js - get it from CDN. Via defs-setup.

# API documentaion

1. To generate human readable documentation install npm documentation globally (`-g`):

```
  npm -g i documentation
```
2. There should be `documentation.yml` file.
    Do command from this folder:

```
  documentation build --config documentation.yml WebAdmin.js -f html -o docs
```

It will generate `docs` folder with the human readable documentation.