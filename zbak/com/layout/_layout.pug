doctype html
html
   head
      //<script src="http://HOST:PROT/vorlon.js"></script>
      script.
         ROOT = '#{ROOT}'
         console.log('#{ROOT}')

         let _start=Date.now()
      script(src='https://cdn.jsdelivr.net/npm/loadjs@3.5.4/dist/loadjs.js')
      script(src='https://cdn.jsdelivr.net/npm/riot@3.11.1/riot.min.js')

      include _header

      block head
      style.
         html { opacity: 0.01; }

   body
      script(src='https://cdn.jsdelivr.net/npm/jquery@3.3.1/dist/jquery.min.js')
      script(src= ROOT+'assets/js/setup.js')

      //- side menu shell
      include _top

      //- container
      #routerFx
      .pusher#router

         //- pushed
         block main

      script.
         console.log('layout')

         loadjs.ready('style', function() {
            setTimeout(function(){
               document.getElementsByTagName('html')[0].style.opacity = 1
            },1000/30)

            $('.ui.sidebar').sidebar('attach events', '.toc.item')

         })//ready
      link(rel='stylesheet', href='/assets/css/semantic1.css', onload='cssLoaded()')

