doctype html
html
   head
      meta(charset='utf-8')
      meta(name='viewport', content='width=device-width, initial-scale=1')
      title #{title}
      script(src=ROOT +'app.js')
      style.
         h1 {
            font-size: 12px;
         }
         h1, button {
            white-space: nowrap;
            text-align: center;
         }
         button {
            padding: 5px 10px;
            border: 0; 
            background: rgba(27, 175, 42, 0.5);
            outline: none;
            color: #fff;
            text-transform: uppercase;
            transition: all .2s ease-in-out;
         }
         button:hover {
            background: rgba(27, 175, 42, 1);
         }

   body
      h1 Map Admin Checker
      button(id="check") Check this page now